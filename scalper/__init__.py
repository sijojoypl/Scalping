"""Reverse RSI forex scalping bot.

A Python port of ``Strategy - Reverse RSI.pine`` with a simulated (paper)
broker. The bot only runs in PAPER mode: it never sends orders to a real
broker.
"""

__version__ = "1.0.0"
